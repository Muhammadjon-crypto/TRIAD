"""
triad.correlation.fft_dock
=============================
PHASE 2 — NOT YET IMPLEMENTED.

Will implement: the core FFT correlation search loop. Explicit outer loop
over rotations (reusing triad.geometry.transforms.sample_rotations), FFT
correlation inner step evaluating all translations simultaneously per
rotation. See docs/TRIAD_v1_MANIFEST.md Part 2.2-2.3 for full spec and the
mandatory validation sequence (synthetic two-sphere test and brute-force
cross-check MUST pass before this touches real protein data).
"""
raise NotImplementedError("Phase 2 module -- see docs/TRIAD_v1_MANIFEST.md")
